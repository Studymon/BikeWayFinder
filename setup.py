from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='Building an app to show the most bike-friendly path for a start-destination route',
    author='Jiazhen Li, Simon Andres',
    license='',
)
